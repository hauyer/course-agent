# 1.1 测试报告

报告日期：2026-07-15。本报告记录本轮缺陷修复后的最终执行结果。

已执行：

- `.venv\Scripts\python.exe -m compileall -q app`：通过。
- 使用隔离 SQLite 与 fake/mock 外部服务执行 `.venv\Scripts\python.exe -m pytest -q`：58 passed（17.90s）。测试导入不再触发对本机 MySQL 的迁移或连接。
- 空 SQLite 数据库 `alembic upgrade head`：到达 `0002_v1_1_features`。
- 从 `v1.0.0-local-backup` 代码创建的 17 表 SQLite 数据库升级：原用户保留，1.1 字段及图谱/综合规划表存在。
- `npm run build`：通过；主入口约 234 KB，Markdown/KaTeX 与 ECharts 图谱独立懒加载。
- 本地 UI 冒烟：验证自定义课程删除弹窗；知识图谱请求不再返回 404；无资料时给出明确提示；知识检索阈值为 35% 且不显示 `NaN`；两门课程综合规划真实生成 600 分钟预览；设置页头像选择区无文字重叠。
- 长对话滚动逻辑改为仅在用户位于消息底部时自动跟随，向上查看历史后不再被强制拉回。
- 本地隔离 UI 稳定性冒烟：旧版不可解密的 Notion Token 不再令集成配置接口返回 500，Obsidian 配置仍可读取；部分成功的知识图谱能展示有效节点；课程问答在切换页面和整页重载后均恢复完整历史消息。
- 后端单文件 EXE 独立冒烟：使用隔离 SQLite、Chroma 目录和 8015 端口启动成功；`/health` 返回 `status=ok`、`app=course-study-desk`、`version=1.1.0`；65 条 API 路由中包含 4 个知识图谱路由、笔记集成配置路由和 Agent 流式问答路由。
- `node --check electron/main.cjs`：通过。
- `electron-builder --win`：通过，生成安装版和便携版 1.1.0；打包资源中的后端 EXE 与本轮后端构建哈希一致。

发行文件：

- `release-1.1.0-stability/课程学习助手-安装版-1.1.0-x64.exe`：447,539,082 bytes；SHA256 `5FEA0D1699A6EF1422F5B198381B41041A24A6E1D1CA66D5A730AA2521F9DFF3`。
- `release-1.1.0-stability/课程学习助手-便携版-1.1.0-x64.exe`：447,323,093 bytes；SHA256 `9FB94C497C49F2F30A9DAB6557845C201ED50EAB91B80D99198381ACC8687B9B`。
- 内置后端 EXE：339,996,859 bytes；SHA256 `827CD920A9C3F35EF2BEEA454123203A92AD1E3FF73518DA9A2C0B2F417A8B1F`；与桌面包内资源完全一致。

当前发行文件未配置代码签名证书，Windows Authenticode 状态为 `NotSigned`。

未调用真实大模型，也未下载真实测试向量模型；相关服务在 pytest 中使用 fake/mock。真实知识图谱生成仍取决于用户重新保存一枚可解密、可用且有余额的 LLM API Key。MySQL 生产数据未执行破坏性迁移测试。
