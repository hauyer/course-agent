# 1.1 测试报告

报告日期：2026-07-15。最终发行结果应以打包结束后的最后一次执行为准。

已执行：

- `python -m compileall -q app alembic scripts`：通过。
- 系统 Python 直接运行测试：因未安装 SQLAlchemy 而在收集阶段退出；该解释器不是项目运行环境。
- `.venv\Scripts\python.exe -m pytest -q`：49 passed（18.73s）。
- 空 SQLite 数据库 `alembic upgrade head`：到达 `0002_v1_1_features`。
- 从 `v1.0.0-local-backup` 代码创建的 17 表 SQLite 数据库升级：原用户保留，1.1 字段及图谱/综合规划表存在。
- `npm run build`：通过；主入口约 234 KB，Markdown/KaTeX 与 ECharts 图谱独立懒加载。
- 本地 UI 冒烟：直接登录、头像设置、课程筛选、图谱入口、资料多选、综合规划、知识检索和课程问答入口均可见且可交互。
- 后端单文件 EXE 独立冒烟：使用隔离 SQLite、Chroma 目录和 8011 端口启动成功；`/health` 返回 `status=ok`，Alembic 自动迁移到 `0002_v1_1_features`，共创建 24 张表。
- `electron-builder --win`：通过，生成安装版和便携版 1.1.0；打包资源包含后端 EXE、`.env.example`、README 与 `app.asar`。

发行文件：

- `课程学习助手-安装版-1.1.0-x64.exe`：447,449,164 bytes；SHA256 `9EFBF0898A7E29FBC0DCE71FB81624910BB286A631BA6D26A4A9B5F3B65E4011`。
- `课程学习助手-便携版-1.1.0-x64.exe`：447,233,172 bytes；SHA256 `37E35F376108F12AD1F43327EA6087DEE014B14699969F618A6E5EB7739DAE0A`。
- 内置后端 EXE：339,991,398 bytes；SHA256 `F1100F5801E793071FC1A590606E0F4B810355C84BC7BE9FC7AB85C16C9544C4`。

当前发行文件未配置代码签名证书，Windows Authenticode 状态为 `NotSigned`。

未调用真实大模型，也未下载真实测试向量模型；相关服务在 pytest 中使用 fake/mock。MySQL 生产数据未执行破坏性迁移测试。
